# ImageSimilarityApp

Web app designed to display semantically similar images to a query image given a collection of input images.

Setup:
1. Clone repo and install dependencies from requirements.txt.
2. Run runserver.py and open http://localhost:5555/
3. Input a collection of images and a query image.
4. Click button "Query" to display the simlar images.
